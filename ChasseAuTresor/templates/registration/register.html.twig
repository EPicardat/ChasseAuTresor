{% extends 'base.html.twig' %}

{% block body %}
    <h1>Inscription !</h1>
    {{ form_start(form, {'action': path('user_registration'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}